---
phase: 08-password-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/ipc/ssh-handlers.ts
  - src/preload/preload.ts
autonomous: true

must_haves:
  truths:
    - "Renderer can check if a stored password exists for a connection"
    - "Renderer can clear a stored password without deleting the connection"
  artifacts:
    - path: "src/main/ipc/ssh-handlers.ts"
      provides: "hasCredential and deleteCredential IPC handlers"
      contains: "ssh:has-credential"
    - path: "src/preload/preload.ts"
      provides: "hasCredential and clearCredential API bindings"
      contains: "hasCredential"
  key_links:
    - from: "src/preload/preload.ts"
      to: "src/main/ipc/ssh-handlers.ts"
      via: "IPC invoke"
      pattern: "ipcRenderer\\.invoke\\('ssh:has-credential'"
---

<objective>
Add IPC handlers for checking and clearing stored credentials.

Purpose: Enable UI to show placeholder dots for stored passwords and provide "clear password" functionality.
Output: Two new IPC endpoints: `ssh:has-credential` and `ssh:clear-credential`
</objective>

<execution_context>
@/Users/yori/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yori/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-password-authentication/08-RESEARCH.md

@src/main/ipc/ssh-handlers.ts
@src/main/storage/credential-store.ts
@src/preload/preload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add credential IPC handlers in main process</name>
  <files>src/main/ipc/ssh-handlers.ts</files>
  <action>
Add two new IPC handlers in registerSSHHandlers():

1. `ssh:has-credential` handler:
   - Takes connectionId: string
   - Returns boolean (true if credential exists)
   - Imports and calls hasCredential() from credential-store.ts

2. `ssh:clear-credential` handler:
   - Takes connectionId: string
   - Returns void
   - Imports and calls deleteCredential() from credential-store.ts
   - Does NOT delete the connection itself, only the credential

Place handlers after the existing ssh:remove-connection handler for logical grouping.

Note: hasCredential and deleteCredential are already exported from credential-store.ts (no changes needed there).
  </action>
  <verify>TypeScript compiles: `cd "/Users/yori/Library/CloudStorage/GoogleDrive-yori.lavi@gmail.com/My Drive/yori/dev/Ubunto-file-explorer" && npm run typecheck`</verify>
  <done>Two IPC handlers registered that delegate to credential-store functions</done>
</task>

<task type="auto">
  <name>Task 2: Expose credential APIs to renderer via preload</name>
  <files>src/preload/preload.ts</files>
  <action>
Add two new methods to the electronAPI object:

1. `hasCredential`:
   - Signature: (connectionId: string) => Promise<boolean>
   - Invokes 'ssh:has-credential' IPC channel

2. `clearCredential`:
   - Signature: (connectionId: string) => Promise<void>
   - Invokes 'ssh:clear-credential' IPC channel

Place these after the existing connection-related methods (addConnection, removeConnection) for logical grouping.
  </action>
  <verify>TypeScript compiles and app starts: `npm run typecheck && npm run start` (can exit after window appears)</verify>
  <done>window.electronAPI.hasCredential and window.electronAPI.clearCredential are available in renderer</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. App starts without console errors
3. In dev tools console: `await window.electronAPI.hasCredential('nonexistent')` returns false
</verification>

<success_criteria>
- hasCredential IPC returns true for connections with stored passwords, false otherwise
- clearCredential IPC removes password without removing connection
- Both APIs accessible via window.electronAPI
</success_criteria>

<output>
After completion, create `.planning/phases/08-password-authentication/08-01-SUMMARY.md`
</output>
