---
phase: 08-password-authentication
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/renderer/components/AddConnectionModal.tsx
  - src/renderer/index.css
autonomous: true

must_haves:
  truths:
    - "User can toggle password visibility with eye icon"
    - "User sees placeholder dots when editing connection with stored password"
    - "User can choose whether to save password via checkbox"
    - "User can clear stored password when editing connection"
  artifacts:
    - path: "src/renderer/components/AddConnectionModal.tsx"
      provides: "Enhanced password field with visibility toggle, save checkbox, clear button"
      min_lines: 350
    - path: "src/renderer/index.css"
      provides: "Styles for password toggle button and save checkbox"
      contains: "form-field__password-toggle"
  key_links:
    - from: "src/renderer/components/AddConnectionModal.tsx"
      to: "window.electronAPI.hasCredential"
      via: "useEffect check on edit"
      pattern: "hasCredential"
    - from: "src/renderer/components/AddConnectionModal.tsx"
      to: "window.electronAPI.clearCredential"
      via: "clear button onClick"
      pattern: "clearCredential"
---

<objective>
Enhance AddConnectionModal with password visibility toggle, save checkbox, stored password indicator, and clear functionality.

Purpose: Complete the password authentication UX per CONTEXT.md decisions.
Output: Full-featured password field with eye toggle, save option, and stored password handling.
</objective>

<execution_context>
~/.gsd/workflows/execute-plan.md
~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-password-authentication/08-CONTEXT.md
@.planning/phases/08-password-authentication/08-RESEARCH.md
@.planning/phases/08-password-authentication/08-01-SUMMARY.md

@src/renderer/components/AddConnectionModal.tsx
@src/renderer/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add password visibility toggle and save checkbox</name>
  <files>src/renderer/components/AddConnectionModal.tsx</files>
  <action>
Enhance the password authentication section of AddConnectionModal:

1. **Add state for password visibility and save preference:**
   ```typescript
   const [showPassword, setShowPassword] = useState(false);
   const [savePassword, setSavePassword] = useState(true); // Default checked per CONTEXT.md
   const [hasStoredPassword, setHasStoredPassword] = useState(false);
   const [passwordModified, setPasswordModified] = useState(false);
   ```

2. **Wrap password input in a container with eye toggle button:**
   - Container div with class `form-field__input-wrapper`
   - Input type toggles between 'password' and 'text' based on showPassword
   - Eye button with class `form-field__password-toggle`
   - Use inline SVG icons for eye (open) and eye-off (slash through)
   - Button toggles showPassword state
   - Button has aria-label for accessibility

3. **Show placeholder when stored password exists:**
   - If hasStoredPassword is true and passwordModified is false:
     - Show placeholder="********" (8 dots/asterisks)
     - Password field value is empty (not the actual password)
   - On first keystroke: set passwordModified to true, clear placeholder behavior

4. **Add "Save password" checkbox below password field:**
   - Only visible when authMethod is 'password'
   - Checkbox with label "Save password securely"
   - Default checked (savePassword: true)
   - Class `form-field__checkbox-row`

5. **Update form submission:**
   - Only pass password to addConnection if savePassword is true
   - If savePassword is false, still pass password for this connection attempt but don't persist

6. **Clear password state when auth method changes:**
   - In handleInputChange, when authMethod changes to non-password:
     - Clear password field
     - Reset showPassword, savePassword, passwordModified states

Eye icon SVGs (from RESEARCH.md):
```tsx
const EyeIcon = () => (
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
    <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
    <circle cx="12" cy="12" r="3"/>
  </svg>
);

const EyeOffIcon = () => (
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
    <path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/>
    <line x1="1" y1="1" x2="23" y2="23"/>
  </svg>
);
```
  </action>
  <verify>TypeScript compiles: `npm run typecheck`</verify>
  <done>Password field has visibility toggle, save checkbox, and clear-on-type behavior for stored passwords</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for password toggle and checkbox</name>
  <files>src/renderer/index.css</files>
  <action>
Add styles for the enhanced password field after the existing form field styles (around line 394):

```css
/* Password field with toggle */
.form-field__input-wrapper {
  position: relative;
  display: flex;
}

.form-field__input-wrapper .form-field__input {
  flex: 1;
  padding-right: 2.5rem; /* Space for toggle button */
}

.form-field__password-toggle {
  position: absolute;
  right: 0.5rem;
  top: 50%;
  transform: translateY(-50%);
  background: none;
  border: none;
  color: #666;
  cursor: pointer;
  padding: 0.25rem;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: color 0.15s;
}

.form-field__password-toggle:hover {
  color: #999;
}

/* Checkbox row for save password */
.form-field__checkbox-row {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-top: 0.25rem;
}

.form-field__checkbox-row input[type="checkbox"] {
  width: 16px;
  height: 16px;
  cursor: pointer;
  accent-color: #3b82f6;
}

.form-field__checkbox-row label {
  font-size: 0.8125rem;
  color: #888;
  cursor: pointer;
}

/* Clear password button */
.form-field__clear-btn {
  background: none;
  border: none;
  color: #888;
  font-size: 0.8125rem;
  cursor: pointer;
  padding: 0;
  text-decoration: underline;
  margin-left: auto;
}

.form-field__clear-btn:hover {
  color: #ef4444;
}
```
  </action>
  <verify>TypeScript/CSS compiles: `npm run typecheck`</verify>
  <done>Password toggle button styled, checkbox row styled, clear button styled</done>
</task>

<task type="auto">
  <name>Task 3: Add stored password detection and clear functionality</name>
  <files>src/renderer/components/AddConnectionModal.tsx</files>
  <action>
This task is for when the modal is used for editing (future feature). For now, implement the infrastructure:

1. **Add props for edit mode (optional, for future use):**
   ```typescript
   interface AddConnectionModalProps {
     isOpen: boolean;
     onClose: () => void;
     onConnectionAdded: () => void;
     editConnectionId?: string; // Optional: if provided, we're editing
   }
   ```

2. **Check for stored credential on mount/when editConnectionId changes:**
   ```typescript
   useEffect(() => {
     if (editConnectionId) {
       window.electronAPI.hasCredential(editConnectionId).then(setHasStoredPassword);
     }
   }, [editConnectionId]);
   ```

3. **Add "Clear saved password" button when hasStoredPassword is true:**
   - Button appears next to or below the save checkbox
   - Text: "Clear saved password"
   - Class: `form-field__clear-btn`
   - onClick: calls `window.electronAPI.clearCredential(editConnectionId)` then sets hasStoredPassword to false

4. **Reset all password state when modal closes:**
   - In handleClose, reset: showPassword, savePassword, passwordModified, hasStoredPassword

Note: The edit modal functionality is not fully implemented yet (out of scope for this phase), but the infrastructure should be in place so the UI components work when edit mode is added later.
  </action>
  <verify>App runs and password auth form works: `npm run start` (manual verification - add connection with password auth, eye toggle works, checkbox works)</verify>
  <done>Infrastructure for stored password detection and clearing is in place, ready for edit mode</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. App starts, can open "Add Connection" modal
3. Select "Password" auth method - password field appears with eye toggle
4. Click eye icon - password becomes visible, icon changes
5. "Save password securely" checkbox is visible and checked by default
6. Uncheck checkbox, add connection - connection saved without password (next connect will need password)
</verification>

<success_criteria>
- AUTH-01: User can connect using password auth (already worked, confirming still works)
- AUTH-02: Password field appears when password auth selected (already worked, enhanced with eye toggle)
- AUTH-03: Password stored via safeStorage when checkbox checked (save checkbox controls this)
- AUTH-04: Auth method remembered per server (already worked via authMethod in connection-store)
</success_criteria>

<output>
After completion, create `.planning/phases/08-password-authentication/08-02-SUMMARY.md`
</output>
