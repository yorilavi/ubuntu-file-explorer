---
phase: 07-hidden-files-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/storage/ui-preferences-store.ts
  - src/main/ipc/ui-preferences-handlers.ts
  - src/preload/preload.ts
autonomous: true

must_haves:
  truths:
    - "Hidden files preference persists across app restarts"
    - "Renderer can read showHiddenFiles preference on mount"
    - "Renderer can save showHiddenFiles preference when changed"
  artifacts:
    - path: "src/main/storage/ui-preferences-store.ts"
      provides: "getShowHiddenFiles and setShowHiddenFiles functions"
      contains: "showHiddenFiles"
    - path: "src/main/ipc/ui-preferences-handlers.ts"
      provides: "IPC handlers for showHiddenFiles"
      contains: "ui:getShowHiddenFiles"
    - path: "src/preload/preload.ts"
      provides: "Typed API for showHiddenFiles"
      contains: "getShowHiddenFiles"
  key_links:
    - from: "src/preload/preload.ts"
      to: "src/main/ipc/ui-preferences-handlers.ts"
      via: "IPC invoke/handle"
      pattern: "ui:getShowHiddenFiles"
    - from: "src/main/ipc/ui-preferences-handlers.ts"
      to: "src/main/storage/ui-preferences-store.ts"
      via: "function import"
      pattern: "getShowHiddenFiles.*setShowHiddenFiles"
---

<objective>
Add persistent storage for the hidden files preference using the existing electron-conf pattern.

Purpose: Users expect the hidden files toggle state to persist across app restarts. This plan adds the backend infrastructure following established patterns.
Output: IPC API that renderer can use to read/write showHiddenFiles preference.
</objective>

<execution_context>
~/.gsd/workflows/execute-plan.md
~/.gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-hidden-files-toggle/07-RESEARCH.md

# Reference existing patterns
@src/main/storage/ui-preferences-store.ts
@src/main/ipc/ui-preferences-handlers.ts
@src/preload/preload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add showHiddenFiles to storage schema</name>
  <files>src/main/storage/ui-preferences-store.ts</files>
  <action>
Add showHiddenFiles boolean to the UIPreferencesSchema interface.
Add default value `showHiddenFiles: false` to the conf defaults (hidden by default matches Finder behavior).
Export two new functions following the existing pattern:
- `getShowHiddenFiles(): boolean` - returns conf.get('showHiddenFiles') with ?? false fallback
- `setShowHiddenFiles(show: boolean): void` - calls conf.set('showHiddenFiles', show)
  </action>
  <verify>File compiles without TypeScript errors: `npx tsc --noEmit src/main/storage/ui-preferences-store.ts`</verify>
  <done>UIPreferencesSchema includes showHiddenFiles, default is false, getter/setter functions exported</done>
</task>

<task type="auto">
  <name>Task 2: Add IPC handlers for showHiddenFiles</name>
  <files>src/main/ipc/ui-preferences-handlers.ts</files>
  <action>
Import the new getShowHiddenFiles and setShowHiddenFiles functions from the storage module.
Inside registerUIPreferencesHandlers(), add two new IPC handlers following the existing column/preview width pattern:
- `ipcMain.handle('ui:getShowHiddenFiles', async () => getShowHiddenFiles())`
- `ipcMain.handle('ui:setShowHiddenFiles', async (_event, show: boolean) => setShowHiddenFiles(show))`
  </action>
  <verify>File compiles without TypeScript errors: `npx tsc --noEmit src/main/ipc/ui-preferences-handlers.ts`</verify>
  <done>Two new IPC handlers registered for get/set showHiddenFiles</done>
</task>

<task type="auto">
  <name>Task 3: Expose showHiddenFiles API in preload</name>
  <files>src/preload/preload.ts</files>
  <action>
In the electronAPI object under the "UI Preferences" section, add two new methods following the existing pattern:
- `getShowHiddenFiles: (): Promise<boolean> => ipcRenderer.invoke('ui:getShowHiddenFiles')`
- `setShowHiddenFiles: (show: boolean): Promise<void> => ipcRenderer.invoke('ui:setShowHiddenFiles', show)`

The ElectronAPI type export will automatically include these since it's `typeof electronAPI`.
  </action>
  <verify>File compiles without TypeScript errors: `npx tsc --noEmit src/preload/preload.ts`</verify>
  <done>Preload exposes getShowHiddenFiles and setShowHiddenFiles on window.electronAPI</done>
</task>

</tasks>

<verification>
1. All three files compile: `npm run lint`
2. App starts without IPC errors: `npm start` (check console for registration message)
3. Quick manual test: In dev tools console, `await window.electronAPI.getShowHiddenFiles()` returns false
</verification>

<success_criteria>
- UIPreferencesSchema has showHiddenFiles: boolean
- Default value is false
- IPC handlers registered for ui:getShowHiddenFiles and ui:setShowHiddenFiles
- Preload exposes getShowHiddenFiles() and setShowHiddenFiles(show)
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-hidden-files-toggle/07-01-SUMMARY.md`
</output>
