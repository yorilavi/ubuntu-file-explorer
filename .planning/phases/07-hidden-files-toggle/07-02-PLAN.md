---
phase: 07-hidden-files-toggle
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/renderer/App.tsx
  - src/renderer/components/HiddenFilesToggle.tsx
  - src/renderer/components/HiddenFilesToggle.css
  - src/renderer/components/FileItem.tsx
  - src/renderer/components/FileItem.css
  - src/renderer/components/ColumnView/Column.tsx
autonomous: true

must_haves:
  truths:
    - "User can press Cmd+Shift+. to toggle hidden files visibility"
    - "User can see toggle state in toolbar via button appearance"
    - "Hidden files appear dimmed (50% opacity) when visible"
    - "Toggle state persists across app restarts"
    - "Toggle only visible when connected to server"
  artifacts:
    - path: "src/renderer/components/HiddenFilesToggle.tsx"
      provides: "Toggle button component with eye icon"
      contains: "HiddenFilesToggle"
    - path: "src/renderer/components/HiddenFilesToggle.css"
      provides: "Toggle button styling with active state"
      contains: "hidden-toggle--active"
    - path: "src/renderer/components/FileItem.css"
      provides: "Dimmed styling for hidden files"
      contains: "file-item--hidden"
    - path: "src/renderer/App.tsx"
      provides: "Keyboard shortcut handler and persistence integration"
      contains: "Period"
  key_links:
    - from: "src/renderer/App.tsx"
      to: "window.electronAPI.getShowHiddenFiles"
      via: "useEffect on mount"
      pattern: "getShowHiddenFiles.*then.*setShowHidden"
    - from: "src/renderer/App.tsx"
      to: "window.electronAPI.setShowHiddenFiles"
      via: "toggle callback"
      pattern: "setShowHiddenFiles"
    - from: "src/renderer/components/HiddenFilesToggle.tsx"
      to: "App.tsx handleToggleHidden"
      via: "onToggle prop"
      pattern: "onToggle.*onClick"
    - from: "src/renderer/components/ColumnView/Column.tsx"
      to: "src/renderer/components/FileItem.tsx"
      via: "isHidden prop"
      pattern: "isHidden.*startsWith"
---

<objective>
Implement the hidden files toggle UI: toolbar button, keyboard shortcut, hidden file styling, and persistence integration.

Purpose: Complete the hidden files toggle feature so users can control dotfile visibility with Cmd+Shift+. or by clicking the toolbar button.
Output: Working toggle feature with visual feedback and persistence.
</objective>

<execution_context>
@/Users/yori/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yori/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-hidden-files-toggle/07-CONTEXT.md
@.planning/phases/07-hidden-files-toggle/07-RESEARCH.md
@.planning/phases/07-hidden-files-toggle/07-01-SUMMARY.md

# Reference existing components
@src/renderer/App.tsx
@src/renderer/components/FileItem.tsx
@src/renderer/components/FileItem.css
@src/renderer/components/ColumnView/Column.tsx
@src/renderer/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HiddenFilesToggle component</name>
  <files>
    src/renderer/components/HiddenFilesToggle.tsx
    src/renderer/components/HiddenFilesToggle.css
  </files>
  <action>
Create a new HiddenFilesToggle component that renders a toolbar button with eye icon.

HiddenFilesToggle.tsx:
- Interface Props: { showHidden: boolean; onToggle: () => void }
- Render a button with className based on showHidden state
- Button has title/tooltip: "Toggle hidden files (Cmd+Shift+.)"
- Button has aria-pressed={showHidden}
- Use inline SVG for eye icon (eye open when showHidden is true, eye closed when false)
- Eye open path: "M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"
- Eye closed path: "M12 7c2.76 0 5 2.24 5 5 0 .65-.13 1.26-.36 1.83l2.92 2.92c1.51-1.26 2.7-2.89 3.43-4.75-1.73-4.39-6-7.5-11-7.5-1.4 0-2.74.25-3.98.7l2.16 2.16C10.74 7.13 11.35 7 12 7zM2 4.27l2.28 2.28.46.46C3.08 8.3 1.78 10.02 1 12c1.73 4.39 6 7.5 11 7.5 1.55 0 3.03-.3 4.38-.84l.42.42L19.73 22 21 20.73 3.27 3 2 4.27zM7.53 9.8l1.55 1.55c-.05.21-.08.43-.08.65 0 1.66 1.34 3 3 3 .22 0 .44-.03.65-.08l1.55 1.55c-.67.33-1.41.53-2.2.53-2.76 0-5-2.24-5-5 0-.79.2-1.53.53-2.2zm4.31-.78l3.15 3.15.02-.16c0-1.66-1.34-3-3-3l-.17.01z"

HiddenFilesToggle.css:
- .hidden-toggle: padding 0.5rem, background none, border none, color #888, cursor pointer
- .hidden-toggle:hover: color #fff
- .hidden-toggle--active: color #3b82f6 (blue to indicate active state)
- .hidden-toggle--active:hover: color #60a5fa
- .hidden-toggle svg: width 18px, height 18px, display block
  </action>
  <verify>Files compile: `npx tsc --noEmit src/renderer/components/HiddenFilesToggle.tsx`</verify>
  <done>HiddenFilesToggle component renders button with eye icon, toggles between open/closed icons based on showHidden prop</done>
</task>

<task type="auto">
  <name>Task 2: Add hidden file styling to FileItem and Column</name>
  <files>
    src/renderer/components/FileItem.tsx
    src/renderer/components/FileItem.css
    src/renderer/components/ColumnView/Column.tsx
  </files>
  <action>
FileItem.tsx:
- Add new prop `isHidden?: boolean` to FileItemProps interface
- Add 'file-item--hidden' to itemClasses when isHidden is true

FileItem.css:
- Add .file-item--hidden with opacity: 0.5
- Add transition: opacity 150ms ease-out to .file-item for smooth appearance

Column.tsx:
- In the virtualizer.getVirtualItems().map() section where FileItem is rendered
- Pass isHidden={entry.name.startsWith('.')} to FileItem component
- This determines hidden status based on the filename starting with dot
  </action>
  <verify>Files compile: `npx tsc --noEmit src/renderer/components/FileItem.tsx src/renderer/components/ColumnView/Column.tsx`</verify>
  <done>FileItem accepts isHidden prop, Column passes it based on filename, hidden files render at 50% opacity</done>
</task>

<task type="auto">
  <name>Task 3: Wire toggle into App.tsx with keyboard shortcut and persistence</name>
  <files>src/renderer/App.tsx</files>
  <action>
1. Import HiddenFilesToggle component

2. Load preference on mount:
   - Change showHidden state type to `boolean | null` initially null
   - Add useEffect to load preference: `window.electronAPI.getShowHiddenFiles().then(setShowHidden)`
   - In render, treat null same as false for ColumnView (or wait until loaded)

3. Create handleToggleHidden callback (useCallback):
   - Toggle the state: setShowHidden(prev => !prev)
   - Save to persistence: window.electronAPI.setShowHiddenFiles(newValue)

4. Add keyboard shortcut useEffect:
   - Listen for keydown events
   - Skip if target is INPUT, TEXTAREA, or contentEditable
   - Check: e.metaKey && e.shiftKey && e.code === 'Period'
   - Call e.preventDefault() and handleToggleHidden()
   - Clean up listener on unmount
   - Add handleToggleHidden to dependency array

5. Replace the checkbox in browser-toolbar__controls:
   - Remove the label and checkbox
   - Add: `<HiddenFilesToggle showHidden={showHidden ?? false} onToggle={handleToggleHidden} />`

6. Pass showHidden to ColumnView (already done, just ensure it uses ?? false for null case)
  </action>
  <verify>
1. `npm run lint` passes
2. `npm start` - app loads without errors
3. Cmd+Shift+. toggles hidden files
4. Click toggle button also works
5. Restart app - preference is remembered
  </verify>
  <done>
- Checkbox replaced with toggle button
- Cmd+Shift+. keyboard shortcut works (global, not when typing in inputs)
- Preference loads on mount and saves on toggle
- Hidden files appear/disappear in all columns
  </done>
</task>

</tasks>

<verification>
1. Start app, connect to server with hidden files present
2. Press Cmd+Shift+. - hidden files should appear (dimmed at 50% opacity)
3. Press Cmd+Shift+. again - hidden files disappear
4. Click toggle button - same behavior
5. With hidden files visible, note the button appears highlighted (blue)
6. Close and reopen app - toggle state preserved
7. Try typing in rename input while pressing Cmd+Shift+. - should NOT toggle (shortcut ignored in inputs)
</verification>

<success_criteria>
- NAV-01: Toggle visibility of dotfiles works
- NAV-02: Cmd+Shift+. keyboard shortcut works (global except in inputs)
- NAV-03: Toggle state persists across sessions
- NAV-04: Toggle state visible via button appearance (blue when active)
- Hidden files render at 50% opacity when visible
- Button shows eye-open icon when showing hidden, eye-closed when hiding
</success_criteria>

<output>
After completion, create `.planning/phases/07-hidden-files-toggle/07-02-SUMMARY.md`
</output>
