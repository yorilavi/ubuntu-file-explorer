---
phase: 03-column-view-navigator
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/renderer/components/FileItem.tsx
  - src/renderer/components/FileItem.css
  - src/renderer/components/ColumnView/Column.tsx
  - src/renderer/components/ColumnView/Column.css
autonomous: true

must_haves:
  truths:
    - "FileItem displays file/folder name with folder chevron indicator"
    - "Column component renders virtualized list of FileItems"
    - "Large directories (1000+ items) scroll smoothly without freezing"
    - "Focused item is visually highlighted and receives keyboard events"
  artifacts:
    - path: "src/renderer/components/FileItem.tsx"
      provides: "Compact file/folder item for Miller column"
      exports: ["default"]
    - path: "src/renderer/components/ColumnView/Column.tsx"
      provides: "Single column with virtualized file list"
      exports: ["default"]
  key_links:
    - from: "src/renderer/components/ColumnView/Column.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer"
      pattern: "useVirtualizer"
    - from: "src/renderer/components/ColumnView/Column.tsx"
      to: "src/renderer/components/FileItem.tsx"
      via: "import FileItem"
      pattern: "import FileItem"
---

<objective>
Create the FileItem component and Column component with virtualized scrolling for displaying directory contents.

Purpose: Build the core visual components that render file listings within each Miller column.
Output: FileItem for individual entries, Column for virtualized list rendering.
</objective>

<execution_context>
@/Users/yori/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yori/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-column-view-navigator/03-RESEARCH.md
@.planning/phases/03-column-view-navigator/03-01-SUMMARY.md
@src/shared/types.ts
@src/renderer/types/columnView.ts
@src/renderer/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FileItem component</name>
  <files>
    src/renderer/components/FileItem.tsx
    src/renderer/components/FileItem.css
  </files>
  <action>
Create a compact file/folder item designed for Miller column display. This is simpler than the existing FileRow (no metadata columns, just icon + name + chevron).

**Create src/renderer/components/FileItem.tsx:**

```typescript
import React from 'react';
import type { FileEntry } from '../../shared/types';
import './FileItem.css';

interface FileItemProps {
  file: FileEntry;
  isSelected: boolean;
  isFocused: boolean;
  onClick: (e: React.MouseEvent) => void;
  onDoubleClick: () => void;
}

/**
 * Compact file/folder item for Miller column display.
 * Shows icon, name, and chevron for folders.
 */
function FileItem({
  file,
  isSelected,
  isFocused,
  onClick,
  onDoubleClick,
}: FileItemProps): React.JSX.Element {
  const itemClasses = [
    'file-item',
    isSelected && 'file-item--selected',
    isFocused && 'file-item--focused',
    file.isDirectory && 'file-item--directory',
  ]
    .filter(Boolean)
    .join(' ');

  return (
    <div
      className={itemClasses}
      onClick={onClick}
      onDoubleClick={onDoubleClick}
      tabIndex={isFocused ? 0 : -1}
      role="option"
      aria-selected={isSelected}
    >
      {/* Icon */}
      <span className={`file-item__icon ${file.isDirectory ? 'file-item__icon--folder' : 'file-item__icon--file'}`}>
        {file.isSymlink && <span className="file-item__symlink-badge" />}
      </span>

      {/* Name */}
      <span className="file-item__name">{file.name}</span>

      {/* Chevron for folders */}
      {file.isDirectory && (
        <span className="file-item__chevron" aria-hidden="true">
          &gt;
        </span>
      )}
    </div>
  );
}

export default React.memo(FileItem);
```

**Create src/renderer/components/FileItem.css:**

```css
/* FileItem - Compact file/folder item for Miller columns */
/* BEM naming convention */

.file-item {
  display: flex;
  align-items: center;
  padding: 0.375rem 0.5rem;
  cursor: pointer;
  transition: background 0.1s;
  height: 28px; /* Fixed height for virtualization */
  user-select: none;
}

.file-item:hover {
  background: #2a2a2a;
}

.file-item--selected {
  background: #2563eb30;
}

.file-item--selected:hover {
  background: #2563eb40;
}

.file-item--focused {
  outline: 2px solid #3b82f6;
  outline-offset: -2px;
}

.file-item--selected.file-item--focused {
  background: #2563eb50;
}

/* Icon */
.file-item__icon {
  position: relative;
  width: 18px;
  height: 18px;
  flex-shrink: 0;
  margin-right: 0.5rem;
}

.file-item__icon--folder::before {
  content: '';
  position: absolute;
  width: 16px;
  height: 12px;
  background: #f59e0b;
  border-radius: 1px 1px 2px 2px;
  top: 3px;
  left: 1px;
}

.file-item__icon--folder::after {
  content: '';
  position: absolute;
  width: 8px;
  height: 3px;
  background: #d97706;
  border-radius: 1px 1px 0 0;
  top: 1px;
  left: 1px;
}

.file-item__icon--file::before {
  content: '';
  position: absolute;
  width: 12px;
  height: 16px;
  background: #666;
  border-radius: 1px;
  top: 1px;
  left: 3px;
}

.file-item__symlink-badge {
  position: absolute;
  bottom: -1px;
  right: -1px;
  width: 8px;
  height: 8px;
  background: #3b82f6;
  border-radius: 50%;
  border: 1.5px solid #1a1a1a;
}

/* Name */
.file-item__name {
  flex: 1;
  font-size: 0.8125rem;
  color: #fff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.file-item--directory .file-item__name {
  font-weight: 500;
}

/* Chevron for folders */
.file-item__chevron {
  flex-shrink: 0;
  margin-left: 0.25rem;
  color: #666;
  font-size: 0.75rem;
  font-weight: bold;
}

.file-item--selected .file-item__chevron,
.file-item:hover .file-item__chevron {
  color: #888;
}
```
  </action>
  <verify>
File exists at src/renderer/components/FileItem.tsx with default export.
CSS file exists with .file-item class and BEM naming.
  </verify>
  <done>FileItem component renders file name, icon, and folder chevron with selection/focus states.</done>
</task>

<task type="auto">
  <name>Task 2: Create Column component with virtualization</name>
  <files>
    src/renderer/components/ColumnView/Column.tsx
    src/renderer/components/ColumnView/Column.css
  </files>
  <action>
Create the Column component that displays a virtualized list of FileItems using @tanstack/react-virtual.

**Create src/renderer/components/ColumnView/Column.tsx:**

```typescript
import React, { useRef, useEffect, useCallback } from 'react';
import { useVirtualizer } from '@tanstack/react-virtual';
import type { FileEntry } from '../../../shared/types';
import type { ColumnState } from '../../types/columnView';
import { useColumnNavigation } from '../../hooks/useColumnNavigation';
import FileItem from '../FileItem';
import './Column.css';

interface ColumnProps {
  columnState: ColumnState;
  columnIndex: number;
  isActive: boolean;
  onItemSelect: (columnIndex: number, itemIndex: number, multi?: boolean, range?: boolean) => void;
  onItemFocus: (columnIndex: number, itemIndex: number) => void;
  onNavigateInto: (columnIndex: number, itemIndex: number) => void;
  onNavigateBack: (columnIndex: number) => void;
  onColumnFocus: (columnIndex: number) => void;
}

/**
 * Single column in the Miller column view.
 * Displays a virtualized list of files/folders with keyboard navigation.
 */
function Column({
  columnState,
  columnIndex,
  isActive,
  onItemSelect,
  onItemFocus,
  onNavigateInto,
  onNavigateBack,
  onColumnFocus,
}: ColumnProps): React.JSX.Element {
  const parentRef = useRef<HTMLDivElement>(null);
  const { path, entries, selectedIndices, focusedIndex, loading, error } = columnState;

  // Virtual scrolling setup
  const virtualizer = useVirtualizer({
    count: entries.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 28, // Fixed row height
    overscan: 10, // Render extra items for smooth scrolling
  });

  // Keyboard navigation
  const { handleKeyDown } = useColumnNavigation({
    itemCount: entries.length,
    focusedIndex,
    virtualizer,
    onFocusChange: (index) => onItemFocus(columnIndex, index),
    onNavigateRight: (index) => onNavigateInto(columnIndex, index),
    onNavigateLeft: () => onNavigateBack(columnIndex),
    onSelect: (index, multi, range) => onItemSelect(columnIndex, index, multi, range),
  });

  // Handle click on item
  const handleItemClick = useCallback(
    (e: React.MouseEvent, index: number) => {
      const multi = e.metaKey || e.ctrlKey;
      const range = e.shiftKey;
      onItemSelect(columnIndex, index, multi, range);
      onItemFocus(columnIndex, index);
    },
    [columnIndex, onItemSelect, onItemFocus]
  );

  // Handle double-click on item
  const handleItemDoubleClick = useCallback(
    (index: number) => {
      const entry = entries[index];
      if (entry?.isDirectory) {
        onNavigateInto(columnIndex, index);
      }
    },
    [columnIndex, entries, onNavigateInto]
  );

  // Focus column container when it becomes active
  useEffect(() => {
    if (isActive && parentRef.current) {
      parentRef.current.focus();
    }
  }, [isActive]);

  // Scroll to focused item when focus changes
  useEffect(() => {
    if (focusedIndex >= 0 && focusedIndex < entries.length) {
      virtualizer.scrollToIndex(focusedIndex, { align: 'auto' });
    }
  }, [focusedIndex, entries.length, virtualizer]);

  const columnClasses = [
    'column',
    isActive && 'column--active',
    loading && 'column--loading',
    error && 'column--error',
  ]
    .filter(Boolean)
    .join(' ');

  // Loading state
  if (loading) {
    return (
      <div className={columnClasses}>
        <div className="column__loading">
          <span className="column__spinner" />
        </div>
      </div>
    );
  }

  // Error state
  if (error) {
    return (
      <div className={columnClasses}>
        <div className="column__error">{error}</div>
      </div>
    );
  }

  // Empty directory
  if (entries.length === 0) {
    return (
      <div className={columnClasses}>
        <div className="column__empty">Empty folder</div>
      </div>
    );
  }

  return (
    <div
      ref={parentRef}
      className={columnClasses}
      tabIndex={0}
      role="listbox"
      aria-label={`Directory ${path}`}
      aria-activedescendant={`item-${columnIndex}-${focusedIndex}`}
      onKeyDown={handleKeyDown}
      onFocus={() => onColumnFocus(columnIndex)}
    >
      <div
        className="column__virtual-container"
        style={{
          height: `${virtualizer.getTotalSize()}px`,
          position: 'relative',
        }}
      >
        {virtualizer.getVirtualItems().map((virtualRow) => {
          const entry = entries[virtualRow.index];
          const isSelected = selectedIndices.has(virtualRow.index);
          const isFocused = virtualRow.index === focusedIndex;

          return (
            <div
              key={virtualRow.key}
              id={`item-${columnIndex}-${virtualRow.index}`}
              data-index={virtualRow.index}
              style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                transform: `translateY(${virtualRow.start}px)`,
              }}
            >
              <FileItem
                file={entry}
                isSelected={isSelected}
                isFocused={isActive && isFocused}
                onClick={(e) => handleItemClick(e, virtualRow.index)}
                onDoubleClick={() => handleItemDoubleClick(virtualRow.index)}
              />
            </div>
          );
        })}
      </div>
    </div>
  );
}

export default React.memo(Column);
```

**Create src/renderer/components/ColumnView/Column.css:**

```css
/* Column - Single column in Miller column view */
/* BEM naming convention */

.column {
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  background: #1a1a1a;
  border-right: 1px solid #333;
  outline: none;
}

.column:focus {
  /* Focus visible on container handled via --active state */
}

.column--active {
  background: #1e1e1e;
}

.column--loading,
.column--error {
  display: flex;
  align-items: center;
  justify-content: center;
}

.column__virtual-container {
  width: 100%;
}

/* Loading state */
.column__loading {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  color: #666;
}

.column__spinner {
  width: 20px;
  height: 20px;
  border: 2px solid #333;
  border-top-color: #3b82f6;
  border-radius: 50%;
  animation: column-spin 0.8s linear infinite;
}

@keyframes column-spin {
  to {
    transform: rotate(360deg);
  }
}

/* Error state */
.column__error {
  padding: 1rem;
  color: #f87171;
  font-size: 0.8125rem;
  text-align: center;
}

/* Empty state */
.column__empty {
  padding: 1rem;
  color: #666;
  font-size: 0.8125rem;
  text-align: center;
}
```

Create the ColumnView directory if it doesn't exist.
  </action>
  <verify>
Column.tsx exists with useVirtualizer from @tanstack/react-virtual.
Column imports and uses FileItem component.
CSS file exists with .column class and loading/error states.
Run `npx tsc --noEmit` - no type errors.
  </verify>
  <done>
Column component renders virtualized list of FileItems.
Keyboard navigation works via useColumnNavigation hook.
Loading spinner and error states display correctly.
  </done>
</task>

</tasks>

<verification>
1. FileItem.tsx and FileItem.css exist with correct exports
2. Column.tsx and Column.css exist in ColumnView directory
3. Column uses useVirtualizer from @tanstack/react-virtual
4. Column uses useColumnNavigation hook from plan 03-01
5. `npx tsc --noEmit` passes without errors
6. CSS follows BEM naming convention established in project
</verification>

<success_criteria>
- FileItem displays icon, name, and chevron for folders
- Column renders virtualized list using @tanstack/react-virtual
- Keyboard navigation hook is wired to Column
- Selection and focus states are visually distinct
- Loading and error states render appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/03-column-view-navigator/03-02-SUMMARY.md`
</output>
