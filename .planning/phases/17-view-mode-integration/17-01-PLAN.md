---
phase: 17-view-mode-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/components/ViewModeToggle.tsx
  - src/renderer/components/ViewModeToggle.css
  - src/main/storage/ui-preferences-store.ts
  - src/main/ipc/ui-preferences-handlers.ts
  - src/preload/preload.ts
autonomous: true

must_haves:
  truths:
    - "ViewModeToggle component renders a button with column/list SVG icons"
    - "viewMode preference persists across app restarts via electron-conf"
    - "Renderer can call getViewMode/setViewMode through IPC bridge"
  artifacts:
    - path: "src/renderer/components/ViewModeToggle.tsx"
      provides: "Toggle button component following HiddenFilesToggle pattern"
      exports: ["default"]
    - path: "src/renderer/components/ViewModeToggle.css"
      provides: "Toggle button styles matching toolbar aesthetic"
      contains: ".view-toggle"
    - path: "src/main/storage/ui-preferences-store.ts"
      provides: "viewMode field in electron-conf schema"
      exports: ["getViewMode", "setViewMode"]
    - path: "src/main/ipc/ui-preferences-handlers.ts"
      provides: "IPC handlers for viewMode get/set"
      contains: "ui:getViewMode"
    - path: "src/preload/preload.ts"
      provides: "getViewMode and setViewMode exposed to renderer"
      contains: "getViewMode"
  key_links:
    - from: "src/renderer/components/ViewModeToggle.tsx"
      to: "ViewModeToggle.css"
      via: "CSS import"
      pattern: "import.*ViewModeToggle\\.css"
    - from: "src/preload/preload.ts"
      to: "ui:getViewMode"
      via: "ipcRenderer.invoke"
      pattern: "ipcRenderer\\.invoke\\('ui:getViewMode'\\)"
    - from: "src/main/ipc/ui-preferences-handlers.ts"
      to: "src/main/storage/ui-preferences-store.ts"
      via: "import getViewMode/setViewMode"
      pattern: "import.*getViewMode.*setViewMode"
---

<objective>
Create the ViewModeToggle component and wire the view mode persistence layer through the Electron IPC bridge.

Purpose: Provides the UI toggle button (VIEW-01) and the persistence plumbing (VIEW-03) that Plan 02 will consume when wiring into App.tsx.
Output: ViewModeToggle.tsx component, ViewModeToggle.css styles, and viewMode get/set available end-to-end from renderer through IPC to electron-conf storage.
</objective>

<execution_context>
@/Users/yori/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yori/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-view-mode-integration/17-RESEARCH.md

# Existing patterns to follow exactly
@src/renderer/components/HiddenFilesToggle.tsx
@src/renderer/components/HiddenFilesToggle.css
@src/main/storage/ui-preferences-store.ts
@src/main/ipc/ui-preferences-handlers.ts
@src/preload/preload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ViewModeToggle component and CSS</name>
  <files>
    src/renderer/components/ViewModeToggle.tsx
    src/renderer/components/ViewModeToggle.css
  </files>
  <action>
Create ViewModeToggle.tsx following the exact structure of HiddenFilesToggle.tsx:

1. **Props interface:** `ViewModeToggleProps` with `viewMode: 'columns' | 'list'` and `onToggle: () => void`.

2. **Component:** Default-exported function `ViewModeToggle`. Render a `<button>` with:
   - `className`: `"view-toggle"` (add `"view-toggle--list"` modifier when `viewMode === 'list'` to allow distinct styling if needed)
   - `onClick={onToggle}`
   - `title`: Show the mode it WILL switch to plus keyboard shortcut. When `viewMode === 'columns'`: `"Switch to list view (Cmd+2)"`. When `viewMode === 'list'`: `"Switch to column view (Cmd+1)"`.
   - `type="button"`
   - Inside the button: an `<svg>` with `viewBox="0 0 24 24"` `fill="currentColor"` `aria-hidden="true"`. The icon shows the CURRENT mode (what you ARE in, not what you switch to):
     - When `viewMode === 'columns'`: Column icon -- three vertical bars: `<path d="M3 3h5v18H3V3zm7 0h4v18h-4V3zm7 0h4v18h-4V3z" />`
     - When `viewMode === 'list'`: List icon -- horizontal lines: `<path d="M3 4h18v2H3V4zm0 5h18v2H3V9zm0 5h18v2H3v-2zm0 5h18v2H3v-2z" />`

3. **CSS file (ViewModeToggle.css):** Follow HiddenFilesToggle.css exactly:
   - `.view-toggle` -- same padding (0.5rem), background none, border none, color #888, cursor pointer, transition color 0.15s ease-out
   - `.view-toggle:hover` -- color #fff
   - `.view-toggle svg` -- width 18px, height 18px, display block
   - No active state needed (unlike HiddenFilesToggle, view mode doesn't have a "highlighted" active vs inactive state -- both modes are equally valid)

Import the CSS file in the component.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors related to ViewModeToggle. Confirm the file exists and exports a default React component.
  </verify>
  <done>
ViewModeToggle.tsx renders a button with two SVG icons (columns/list) that switches based on the viewMode prop. CSS matches HiddenFilesToggle styling. Component is ready to import in App.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add viewMode persistence through IPC bridge</name>
  <files>
    src/main/storage/ui-preferences-store.ts
    src/main/ipc/ui-preferences-handlers.ts
    src/preload/preload.ts
  </files>
  <action>
Follow the exact showHiddenFiles pattern across all three files:

1. **ui-preferences-store.ts:**
   - Add `viewMode: 'columns' | 'list'` to the `UIPreferencesSchema` interface
   - Add `viewMode: 'columns'` to the `defaults` object (columns is the existing default behavior)
   - Add `getViewMode(): 'columns' | 'list'` function that returns `conf.get('viewMode') ?? 'columns'`
   - Add `setViewMode(mode: 'columns' | 'list'): void` function that calls `conf.set('viewMode', mode)`

2. **ui-preferences-handlers.ts:**
   - Add `getViewMode` and `setViewMode` to the import from `'../storage/ui-preferences-store'`
   - Add two handlers inside `registerUIPreferencesHandlers()`:
     - `ipcMain.handle('ui:getViewMode', async (): Promise<string> => { return getViewMode(); });`
     - `ipcMain.handle('ui:setViewMode', async (_event, mode: string): Promise<void> => { setViewMode(mode as 'columns' | 'list'); });`

3. **preload.ts:**
   - In the `electronAPI` object, under the "UI Preferences" section, add two methods right after the `setShowHiddenFiles` entry:
     - `getViewMode: (): Promise<string> => ipcRenderer.invoke('ui:getViewMode'),`
     - `setViewMode: (mode: string): Promise<void> => ipcRenderer.invoke('ui:setViewMode', mode),`
   - Add JSDoc comments matching the existing style: `/** Get view mode preference. */` and `/** Save view mode preference. */`

Note: The `ElectronAPI` type is automatically inferred from `typeof electronAPI` in preload.ts, and `Window.electronAPI` gets the type via `src/shared/types.ts` import. No additional type declarations needed -- adding the methods to the `electronAPI` object is sufficient.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors. Check that `window.electronAPI.getViewMode` and `window.electronAPI.setViewMode` are recognized by TypeScript (the shared/types.ts Window extension picks them up automatically).
  </verify>
  <done>
viewMode persists via electron-conf with 'columns' default. IPC handlers registered for get/set. Renderer can call `window.electronAPI.getViewMode()` and `window.electronAPI.setViewMode('list')`. The full persistence chain (store -> handlers -> preload -> renderer) is complete and type-safe.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. ViewModeToggle.tsx exists and exports a default component
3. ViewModeToggle.css exists with .view-toggle class
4. ui-preferences-store.ts exports getViewMode and setViewMode
5. ui-preferences-handlers.ts registers ui:getViewMode and ui:setViewMode handlers
6. preload.ts exposes getViewMode and setViewMode on electronAPI
</verification>

<success_criteria>
- ViewModeToggle component renders correctly with both 'columns' and 'list' props
- Persistence layer follows the proven showHiddenFiles pattern across all 3 Electron layers
- TypeScript compilation succeeds with all new code
</success_criteria>

<output>
After completion, create `.planning/phases/17-view-mode-integration/17-01-SUMMARY.md`
</output>
