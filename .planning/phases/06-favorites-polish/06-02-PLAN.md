---
phase: 06-favorites-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/components/ToastProvider.tsx
  - src/renderer/App.tsx
  - src/renderer/index.css
autonomous: true

must_haves:
  truths:
    - "Toast notifications appear for user feedback"
    - "Progress toasts show file transfer progress"
    - "Error toasts display user-friendly messages with actions"
  artifacts:
    - path: "src/renderer/components/ToastProvider.tsx"
      provides: "Sonner Toaster component with app styling"
      exports: ["ToastProvider"]
    - path: "src/renderer/App.tsx"
      provides: "ToastProvider integrated at app root"
      contains: "ToastProvider"
  key_links:
    - from: "src/renderer/App.tsx"
      to: "src/renderer/components/ToastProvider.tsx"
      via: "import and render"
      pattern: "import.*ToastProvider"
---

<objective>
Set up toast notification infrastructure using sonner.

Purpose: Provide visual feedback for file operations (progress, success, errors) that can be cancelled.
Output: ToastProvider component integrated into App, ready for use by file operations.
</objective>

<execution_context>
@/Users/yori/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yori/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-favorites-polish/06-RESEARCH.md

# App integration point
@src/renderer/App.tsx
@src/renderer/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToastProvider component</name>
  <files>
    src/renderer/components/ToastProvider.tsx
  </files>
  <action>
    Create ToastProvider.tsx that wraps sonner's Toaster:

    ```typescript
    import { Toaster } from 'sonner';

    /**
     * Toast notification provider using sonner.
     * Configured for bottom-right positioning with app styling.
     */
    export function ToastProvider(): React.JSX.Element {
      return (
        <Toaster
          position="bottom-right"
          expand={false}
          richColors
          closeButton
          toastOptions={{
            duration: 4000,
            className: 'app-toast',
          }}
        />
      );
    }
    ```

    Configuration choices per RESEARCH.md:
    - position: bottom-right (doesn't overlap sidebar or preview)
    - expand: false (compact, doesn't take over screen)
    - richColors: true (success=green, error=red)
    - closeButton: true (allows manual dismissal)
    - duration: 4000ms default (errors will override to longer)
  </action>
  <verify>
    TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
    ToastProvider component created with sonner configuration
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate ToastProvider into App and add styles</name>
  <files>
    src/renderer/App.tsx
    src/renderer/index.css
  </files>
  <action>
    1. Update App.tsx:
       - Import ToastProvider: `import { ToastProvider } from './components/ToastProvider';`
       - Add ToastProvider at root level (after closing div but before fragment close, or wrap in fragment):
         ```tsx
         return (
           <>
             <div className="app-layout">
               {/* existing content */}
             </div>
             <ToastProvider />
             {/* Lightbox overlay */}
             {lightboxSrc && (
               <LightboxView ... />
             )}
           </>
         );
         ```

    2. Add toast styles to index.css at the end:
       ```css
       /* Toast styles */
       .app-toast {
         font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
         font-size: 13px;
       }

       /* Ensure toasts appear above other overlays */
       [data-sonner-toaster] {
         z-index: 10000;
       }

       /* Progress bar within toast */
       .toast-progress {
         height: 4px;
         background: var(--selection-color, #0078d4);
         border-radius: 2px;
         margin-top: 8px;
         transition: width 0.3s ease;
       }
       ```
  </action>
  <verify>
    - App compiles and starts: `npm start`
    - Test toast appears: In DevTools console run `import('sonner').then(m => m.toast.success('Test!'))`
    - Toast appears bottom-right with close button
  </verify>
  <done>
    ToastProvider rendered in App, toast styles applied, test toast displays correctly
  </done>
</task>

</tasks>

<verification>
1. App starts without errors
2. Toaster renders (invisible until toast fires)
3. Manual toast test via DevTools shows notification bottom-right
4. Toast has close button and auto-dismisses after 4 seconds
</verification>

<success_criteria>
- ToastProvider component exists and is rendered in App.tsx
- Toast notifications can be triggered from anywhere in the app
- Styling matches app theme (font family, appropriate z-index)
</success_criteria>

<output>
After completion, create `.planning/phases/06-favorites-polish/06-02-SUMMARY.md`
</output>
