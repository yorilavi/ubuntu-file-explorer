---
phase: 06-favorites-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main/storage/favorites-store.ts
  - src/main/ipc/favorites-handlers.ts
  - src/main/main.ts
  - src/preload/preload.ts
autonomous: true

must_haves:
  truths:
    - "Favorites can be added, removed, and reordered via IPC from renderer"
    - "Favorites persist between app sessions"
    - "Each server has its own independent favorites array"
  artifacts:
    - path: "src/main/storage/favorites-store.ts"
      provides: "Per-server favorites CRUD operations"
      exports: ["addFavorite", "removeFavorite", "getFavorites", "reorderFavorites"]
    - path: "src/main/ipc/favorites-handlers.ts"
      provides: "IPC handlers for favorites operations"
      exports: ["registerFavoritesHandlers"]
  key_links:
    - from: "src/main/ipc/favorites-handlers.ts"
      to: "src/main/storage/favorites-store.ts"
      via: "import and call store functions"
      pattern: "favoritesStore\\.(add|remove|get|reorder)"
    - from: "src/preload/preload.ts"
      to: "favorites:*"
      via: "ipcRenderer.invoke"
      pattern: "ipcRenderer\\.invoke\\('favorites:"
---

<objective>
Install dependencies and create favorites storage with IPC bridge.

Purpose: Enable renderer to persist per-server favorites via main process, following established electron-conf pattern.
Output: Favorites store module, IPC handlers, preload API extensions.
</objective>

<execution_context>
@/Users/yori/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yori/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-favorites-polish/06-RESEARCH.md

# Existing storage pattern to follow
@src/main/storage/connection-store.ts

# IPC registration pattern
@src/main/main.ts
@src/preload/preload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create favorites store</name>
  <files>
    package.json
    src/main/storage/favorites-store.ts
  </files>
  <action>
    1. Install sonner and dnd-kit packages (for later plans, install now to avoid multiple npm runs):
       `npm install sonner @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`

    2. Create favorites-store.ts following connection-store.ts pattern:
       - Use electron-conf with FavoritesStoreSchema
       - Schema: `{ favorites: Record<string, string[]> }` where key is serverId, value is array of folder paths
       - Export functions:
         - `getFavorites(serverId: string): string[]` - returns favorites array for server
         - `addFavorite(serverId: string, path: string): void` - adds path if not exists
         - `removeFavorite(serverId: string, path: string): void` - removes path
         - `reorderFavorites(serverId: string, paths: string[]): void` - replaces entire array (for drag reorder)
       - Use separate Conf instance from connection-store (electron-conf supports multiple instances)
       - Console.log operations for debugging like connection-store does
  </action>
  <verify>
    - npm install completes without errors
    - TypeScript compiles: `npx tsc --noEmit`
    - File exists with all 4 exports
  </verify>
  <done>
    Packages installed, favorites-store.ts exports getFavorites, addFavorite, removeFavorite, reorderFavorites
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IPC handlers and extend preload</name>
  <files>
    src/main/ipc/favorites-handlers.ts
    src/main/main.ts
    src/preload/preload.ts
  </files>
  <action>
    1. Create favorites-handlers.ts:
       - Export `registerFavoritesHandlers()` function (no mainWindow needed unlike file-ops)
       - Register 4 IPC handlers:
         - `favorites:get` -> getFavorites(serverId)
         - `favorites:add` -> addFavorite(serverId, path)
         - `favorites:remove` -> removeFavorite(serverId, path)
         - `favorites:reorder` -> reorderFavorites(serverId, paths)
       - All handlers async (even if functions are sync) for consistency

    2. Update main.ts:
       - Import registerFavoritesHandlers
       - Call it in the IPC registration section (before createWindow or with other handlers)

    3. Update preload.ts:
       - Add 4 methods to electronAPI object:
         - `getFavorites: (serverId: string): Promise<string[]>`
         - `addFavorite: (serverId: string, path: string): Promise<void>`
         - `removeFavorite: (serverId: string, path: string): Promise<void>`
         - `reorderFavorites: (serverId: string, paths: string[]): Promise<void>`
       - Add comment section header "// Favorites Operations" like existing sections
  </action>
  <verify>
    - TypeScript compiles: `npx tsc --noEmit`
    - App starts: `npm start` (verify no console errors)
  </verify>
  <done>
    IPC handlers registered, preload exposes favorites methods, app starts without errors
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. App starts and renders correctly
3. Console shows no errors on startup
4. Favorites operations available on window.electronAPI (verify in DevTools console)
</verification>

<success_criteria>
- sonner and @dnd-kit packages in package.json dependencies
- favorites-store.ts exists with typed electron-conf storage
- IPC bridge complete: renderer can call window.electronAPI.getFavorites(), addFavorite(), etc.
- No runtime errors on app startup
</success_criteria>

<output>
After completion, create `.planning/phases/06-favorites-polish/06-01-SUMMARY.md`
</output>
